import XCTest
@testable import Flat_Land_Server

final class Flat_Land_ServerTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Flat_Land_Server().text, "Hello, World!")
    }


    static var allTests = [
        ("testExample", testExample),
    ]
}
